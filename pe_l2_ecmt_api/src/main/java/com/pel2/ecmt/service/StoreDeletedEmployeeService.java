package com.pel2.ecmt.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Service;

@Service
public class StoreDeletedEmployeeService 
{/*
	
	@Autowired
	Dao dao;
	
	
	public boolean storeEmployee(String empId, TerminationInfo termInfo)
	{
		Profile profile = dao.getEmployee(empId);
		boolean result = dao.insertPreviousEmployee(profile, termInfo);
		return result;
	}

*/}
